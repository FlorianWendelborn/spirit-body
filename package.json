{
  "author": "Florian Wendelborn",
  "babel": {
    "presets": [
      "es2015"
    ]
  },
  "bugs": {
    "url": "https://github.com/dodekeract/spirit-body/issues"
  },
  "description": "Body parser for spirit.",
  "devDependencies": {
    "babel-cli": "^6.16.0",
    "babel-preset-es2015": "^6.16.0",
    "chai": "^3.5.0",
    "coveralls": "^2.12.0",
    "istanbul": "^0.4.5",
    "mocha": "^3.2.0",
    "nodemon": "^1.11.0"
  },
  "homepage": "https://github.com/dodekeract/spirit-body#readme",
  "keywords": [
    "spirit",
    "body",
    "spiritjs",
    "spirit-js",
    "parser",
    "json",
    "xml",
    "post",
    "delete",
    "put"
  ],
  "license": "MIT",
  "main": "build/",
  "name": "spirit-body",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dodekeract/spirit-body.git"
  },
  "scripts": {
    "build": "babel source -d build",
    "postpublish": "PACKAGE_VERSION=$(node -e \"console.log(require('./package.json').version)\") && git tag $PACKAGE_VERSION && git push --tags",
    "test": "yarn run test-build && mocha build/test",
    "test-build": "yarn run build && babel test -d build/test",
    "travis": "yarn run test-build && istanbul cover ./node_modules/mocha/bin/_mocha -- -R spec build/test"
  },
  "version": "1.0.0"
}
